/**
 * Единый источник fallback-значений для цветов
 * Синхронизировано с ColorSystem.CONFIG.accents[0]
 */

import ColorSystem from './ColorSystem.js';

export const DEFAULT_ACCENT = ColorSystem.CONFIG.accents[0].value;
